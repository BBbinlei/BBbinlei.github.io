# 静态素材说明

1. **背景视频**:
   - 将 MiniMax 生成的 10~15 秒循环视频重命名为 `hero-bg.mp4` 并放置在 `public/videos/hero-bg.mp4`。
   - 系统检测到该文件后会自动在首屏以 35% 不透明度播放；若无此文件，系统会自动回退为极致优雅的纯 Canvas 星光与深蓝流光微粒动画。

2. **简历文件**:
   - 将你的 PDF 简历放置在 `public/resume-binlei.pdf`。
   - 网站各处的“下载 PDF 简历”按钮均直接指向此路径。
